# The example corpus declaration (ERF-59).
#
# The declaration travels with the corpus, which is what makes a received
# corpus self-describing. `classification` is an opaque label the format
# records and does not read (ERF-59); this corpus carries one to show the
# field, and a corpus that omits it is exactly as conforming.

id: erf-example
title: "ERF example corpus"
spec_version: "0.9.0"
classification: public

owner: "human:fbouet"
