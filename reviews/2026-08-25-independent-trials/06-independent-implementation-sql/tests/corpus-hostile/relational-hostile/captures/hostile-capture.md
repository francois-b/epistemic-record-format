# Hostile fixture capture

Constructed material for the SQL trial.

The writer's chosen style is not the format's style, because the format has no
style; a file that parses to the same mapping is the same record under every
reading the document supports.

Nothing in the serialization section constrains the order in which a producer
writes the keys of a record.
