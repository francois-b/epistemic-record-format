requirement: YAMLB-3
record: fx-001
field: (file)
because: >
  The file opens with a fence and no later line is exactly `---`. It is a
  document that fails the grammar, reported rather than read as a record
  with empty frontmatter or dropped as unrecognized.
