requirement: ERF-4
record: fx-001
field: captures.yaml
because: >
  Absence must be explicit. A mapping is checkable only when it is complete,
  because a validator can tell a recorded absence from an omission and cannot
  tell an omission from an oversight.
