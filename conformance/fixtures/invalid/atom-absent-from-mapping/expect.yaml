requirement: ERF-35
record: fx-001
field: source
because: >
  Every atom names its source, and the named id must exist in the corpus's
  source list. Absence must be explicit: a list is checkable only when it is
  complete, because a validator can tell a recorded absence from an omission
  and cannot tell an omission from an oversight.
