requirement: ERF-5
record: fx-001
field: captures.yaml
because: >
  An entry recording an absence carries a reason from the closed set and a
  human-readable note, so a reader learns why the check cannot run rather
  than only that it did not.
