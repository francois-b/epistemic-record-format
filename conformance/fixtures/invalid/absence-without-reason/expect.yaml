requirement: ERF-73
record: fx-src
field: reason
because: >
  A source recording an absence carries a reason from the closed set and a
  human-readable note, so a reader learns why the check cannot run rather
  than only that it did not.
