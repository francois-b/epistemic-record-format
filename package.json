{
  "name": "epistemic-record-format",
  "private": true,
  "version": "0.9.0",
  "description": "The Epistemic Record Format: specification, reference validator and viewer, conformance suite.",
  "license": "Apache-2.0",
  "scripts": {
    "postinstall": "git config core.hooksPath .githooks && npm install --prefix validator && npm install --prefix tools/viewer && npm install --prefix conformance",
    "check": "validator/node_modules/.bin/tsx validator/erf-check.ts",
    "view": "tools/viewer/node_modules/.bin/tsx tools/viewer/erf-view.ts",
    "test": "npm test --prefix conformance",
    "site": "./site/build.sh",
    "types": "python3 tools/generate-types.py --write"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.0"
  }
}
