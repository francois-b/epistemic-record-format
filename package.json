{
  "name": "epistemic-record-format",
  "private": true,
  "version": "0.9.0",
  "description": "The Epistemic Record Format: specification, reference validator and viewer, conformance suite.",
  "license": "Apache-2.0",
  "scripts": {
    "postinstall": "git config core.hooksPath .githooks && npm install --prefix viewer && npm install --prefix conformance",
    "check": "viewer/node_modules/.bin/tsx viewer/erf-check.ts",
    "view": "viewer/node_modules/.bin/tsx viewer/erf-view.ts",
    "test": "npm test --prefix conformance",
    "site": "./site/build.sh",
    "types": "python3 tools/generate-types.py --write"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.0"
  }
}
